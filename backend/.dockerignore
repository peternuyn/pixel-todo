# Files Docker should NOT copy into the build context.
# Keeping this list tight makes builds faster (less to send to the Docker daemon)
# and avoids leaking local junk/secrets into the image.

target/          # local Maven build output — we rebuild inside the image
.git/
.gitignore
.idea/
*.iml
.vscode/
*.log
HELP.md
CLAUDE.md
